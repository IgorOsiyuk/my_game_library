export type FontWeights = {
  light: number;
  regular: number;
  medium: number;
  semiBold: number;
  bold: number;
};

const fontWeights: FontWeights = {
  light: 300,
  regular: 400,
  medium: 500,
  semiBold: 600,
  bold: 700,
};

export default fontWeights;
