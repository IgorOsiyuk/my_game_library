export type Radius = {
  rounded_large: string;
  rounded_medium: string;
  rounded_small: string;
};

const radius: Radius = {
  rounded_large: '24px',
  rounded_medium: '12px',
  rounded_small: '8px',
};

export default radius;
