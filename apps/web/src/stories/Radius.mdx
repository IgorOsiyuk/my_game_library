import { theme } from '@/styles/theme';
import { Meta, Canvas } from '@storybook/blocks';

<Meta title="Radius" />

# Rounded

export const Spacing = ({ spacing }) => {
  return (
    <>
    {Object.entries(spacing).map(([key,value])=>
      <div
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent:"center",
        }}
      >    
        <div
          style={{
            background: theme.colors.grey.light,
            flexGrow: 1,
            flexSrink:0,
            borderRadius: value,
            opacity:.2,
            height: `calc(8*${value})`,
            width: `calc(8*${value})`,
          }}
        />
        <div>{key}</div>
        <div>{value}</div>
      </div>
    )}
    </>

);
};

<div
  style={{
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'end',
    gap: 10,
  }}
>
  <Spacing spacing={theme.radius} />
</div>
