import colors from '@/styles/colors';
import fontSizes from '@/styles/fontSizes';
import { theme } from '@/styles/theme';
import { Meta, Typeset, ColorPalette, ColorItem } from '@storybook/blocks';

<Meta title="Colors" />

# Colors

<ColorPalette>
  {Object.keys(colors).map((el) => (
    <>
      <ColorItem title={`theme.colors.${el}.light`} subtitle={el} colors={[theme.colors[el].light]} />
      <ColorItem title={`theme.colors.${el}.dark`} colors={[theme.colors[el].dark]} />
    </>
  ))}
</ColorPalette>
