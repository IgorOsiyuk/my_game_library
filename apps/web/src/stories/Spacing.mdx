import { theme } from '@/styles/theme';
import { Meta, Canvas } from '@storybook/blocks';

<Meta title="Spacing" />

# Spacing

export const Spacing = ({ spacing }) => {
  return (
    <>
    {Object.entries(spacing).map(([key,value])=>
      <div
        style={{
          display: 'grid',
          gridTemplateColumns:"2fr 18fr 2fr",
          gap: 20,
          alignItems: 'center',
          justifyContent:"center",
        }}
      >
        <div>{key}</div>
        <div
          style={{
            background: theme.colors.accent.light,
            flexGrow: 1,
            height: value,
            opacity:.2
          }}
        />
        <div>{value}</div>
      </div>
    )}
    </>

);
};

<div
  style={{
    display: 'flex',
    flexDirection: 'column',
    gap: 10,
  }}
>
  <Spacing spacing={theme.spacing} />
</div>
